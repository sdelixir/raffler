<h2>New entrant</h2>

<%= render "form.html", changeset: @changeset,
                        raffle_id: @raffle_id,
                        action: entrant_path(@conn, :create) %>

<%= link "Back", to: raffle_path(@conn, :show, @raffle_id) %>
